# top_mem
Small monolithic C program to that reads the /proc virtual file system and finds both memory utilization and the top memory using processes. Output is in JSON, meant to be used withing a Waybar custom memory module.
